diff --git a/node_modules/node-llama-cpp/dist/bindings/AddonTypes.d.ts b/node_modules/node-llama-cpp/dist/bindings/AddonTypes.d.ts
index 89a6494..3fe5cd3 100644
--- a/node_modules/node-llama-cpp/dist/bindings/AddonTypes.d.ts
+++ b/node_modules/node-llama-cpp/dist/bindings/AddonTypes.d.ts
@@ -147,6 +147,12 @@ export type AddonContext = {
     loadSequenceStateFromFile(filePath: string, sequenceId: number, maxContextSize: number): Promise<Uint32Array>;
     setLoras(loras: AddonModelLora[], scales: number[]): void;
     restoreCheckpoint(checkpoint: AddonContextSequenceCheckpoint, maxPosIndex: number): Promise<boolean>;
+    /** QRRanker: get collected kq_soft_max attention tensor data for a layer (0-indexed). Returns Float32Array or undefined. */
+    getKqSoftMax(layer: number): Float32Array | undefined;
+    /** QRRanker: get shape metadata of collected kq_soft_max tensors. */
+    getKqSoftMaxShape(): { nKv: number; nTokens: number; nHead: number; nLayers: number; layers: number[] };
+    /** QRRanker: enable/disable kq_soft_max collection at runtime. */
+    setCollectKqSoftMax(val: boolean): void;
 };
 export type AddonContextSequenceCheckpoint = {
     init(context: AddonContext, sequenceId: number): Promise<void>;
diff --git a/node_modules/node-llama-cpp/dist/cli/cli.js b/node_modules/node-llama-cpp/dist/cli/cli.js
old mode 100755
new mode 100644
diff --git a/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.d.ts b/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.d.ts
index 905b41c..ae5c8f5 100644
--- a/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.d.ts
+++ b/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.d.ts
@@ -122,6 +122,17 @@ export declare class LlamaContext {
      * it won't print anything.
      */
     printTimings(): Promise<void>;
+    /**
+     * QRRanker: Get collected kq_soft_max attention tensor data for a specific layer.
+     * Returns a Float32Array containing the attention weights, or undefined if not collected.
+     * Layer index is 0-based.
+     */
+    getKqSoftMax(layer: number): Float32Array | undefined;
+    /**
+     * QRRanker: Get shape metadata of all collected kq_soft_max tensors.
+     * Returns the tensor dimensions and list of layers that were collected.
+     */
+    getKqSoftMaxShape(): { nKv: number; nTokens: number; nHead: number; nLayers: number; layers: number[] };
 }
 export declare class LlamaContextSequence {
     readonly onDispose: EventRelay<void>;
diff --git a/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.js b/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.js
index f830b08..04644b8 100644
--- a/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.js
+++ b/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/LlamaContext.js
@@ -77,7 +77,7 @@ export class LlamaContext {
     /** @internal */ _allocatedContextSize;
     /** @internal */ _disposed = false;
     onDispose = new EventRelay();
-    constructor({ _model }, { sequences, contextSize, batchSize, flashAttention = _model.defaultContextFlashAttention, threads, batching: { dispatchSchedule: batchingDispatchSchedule = "nextCycle", itemPrioritizationStrategy: batchingItemsPrioritizationStrategy = "maximumParallelism" } = {}, swaFullCache = _model.defaultContextSwaFullCache, performanceTracking = false, experimentalKvCacheKeyType, experimentalKvCacheValueType, _embeddings, _ranking }) {
+    constructor({ _model }, { sequences, contextSize, batchSize, flashAttention = _model.defaultContextFlashAttention, threads, batching: { dispatchSchedule: batchingDispatchSchedule = "nextCycle", itemPrioritizationStrategy: batchingItemsPrioritizationStrategy = "maximumParallelism" } = {}, swaFullCache = _model.defaultContextSwaFullCache, performanceTracking = false, experimentalKvCacheKeyType, experimentalKvCacheValueType, _embeddings, _ranking, collectKqSoftMax = false }) {
         if (_model.disposed)
             throw new DisposedError();
         this._llama = _model._llama;
@@ -113,7 +113,8 @@ export class LlamaContext {
             performanceTracking: this._performanceTracking,
             kvCacheKeyType: this._kvCacheKeyType,
             kvCacheValueType: this._kvCacheValueType,
-            swaFullCache: this._swaFullCache
+            swaFullCache: this._swaFullCache,
+            collectKqSoftMax: collectKqSoftMax
         }));
         this._batchingOptions = {
             dispatchSchedule: batchingDispatchSchedule,
@@ -474,6 +475,19 @@ export class LlamaContext {
         await new Promise((accept) => setTimeout(accept, 0)); // wait for the logs to finish printing
     }
     /** @internal */
+    /**
+     * QRRanker: Get collected kq_soft_max attention tensor data for a specific layer.
+     * Layer is 0-indexed. Returns Float32Array or undefined.
+     */
+    getKqSoftMax(layer) {
+        return this._ctx.getKqSoftMax(layer);
+    }
+    /**
+     * QRRanker: Get shape metadata of collected kq_soft_max tensors.
+     */
+    getKqSoftMaxShape() {
+        return this._ctx.getKqSoftMaxShape();
+    }
     async _decodeTokens({ sequenceId, firstTokenSequenceIndex, tokens, logits, evaluationPriority = defaultEvaluationPriority, tokenMeter, afterBatchAction }, logitDataMapper) {
         return await new Promise((accept, reject) => {
             this._queuedDecodes.push({
diff --git a/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/types.d.ts b/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/types.d.ts
index 93c9ea6..85dd7c1 100644
--- a/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/types.d.ts
+++ b/node_modules/node-llama-cpp/dist/evaluator/LlamaContext/types.d.ts
@@ -141,6 +141,14 @@ export type LlamaContextOptions = {
      * Defaults to `false` (inherited from the model option `defaultContextSwaFullCache`);
      */
     swaFullCache?: boolean;
+    /**
+     * QRRanker: enable collection of kq_soft_max attention tensors during decode.
+     * When enabled, forces flash attention off and collects intermediate attention weights
+     * for QR score computation.
+     *
+     * Defaults to `false`.
+     */
+    collectKqSoftMax?: boolean;
     /**
      * Load the provided LoRA adapters onto the context.
      * LoRA adapters are used to modify the weights of a pretrained model to adapt to new tasks or domains
diff --git a/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.d.ts b/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.d.ts
index d60237d..bce642a 100644
--- a/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.d.ts
+++ b/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.d.ts
@@ -44,6 +44,13 @@ export declare class LlamaEmbeddingContext {
     readonly onDispose: EventRelay<void>;
     private constructor();
     getEmbeddingFor(input: Token[] | string | LlamaText): Promise<LlamaEmbedding>;
+    /**
+     * Returns per-token embeddings for the given input.
+     * Requires a model with pooling_type=none in GGUF metadata.
+     * @returns Promise resolving to an array of per-token embedding vectors (each is number[])
+     */
+    getEmbeddingsForTokens(input: Token[] | string | LlamaText): Promise<number[][]>;
+
     dispose(): Promise<void>;
     /** @hidden */
     [Symbol.asyncDispose](): Promise<void>;
diff --git a/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.js b/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.js
index 20b5a3d..43949fe 100644
--- a/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.js
+++ b/node_modules/node-llama-cpp/dist/evaluator/LlamaEmbeddingContext.js
@@ -53,6 +53,61 @@ export class LlamaEmbeddingContext {
             });
         });
     }
+    /**
+     * Returns per-token embeddings for the given input.
+     * Requires a model with pooling_type=none in GGUF metadata.
+     * Each element is a Float32Array representing the hidden state at that token position.
+     * @param input - text or token array
+     * @returns Promise resolving to an array of per-token embedding vectors
+     */
+    async getEmbeddingsForTokens(input) {
+        const resolvedInput = tokenizeInput(input, this._llamaContext.model.tokenizer, undefined, true);
+        if (resolvedInput.length > this._llamaContext.contextSize)
+            throw new Error("Input is longer than the context size. " +
+                "Try to increase the context size or use another model that supports longer contexts.");
+        else if (resolvedInput.length === 0)
+            return [];
+        const beginningToken = resolveBeginningTokenToPrepend(this.model.vocabularyType, this.model.tokens);
+        if (beginningToken != null && resolvedInput[0] !== beginningToken)
+            resolvedInput.unshift(beginningToken);
+        const endToken = resolveEndTokenToAppend(this.model.vocabularyType, this.model.tokens);
+        if (endToken != null && resolvedInput.at(-1) !== endToken)
+            resolvedInput.push(endToken);
+        return await withLock([this, "evaluate"], async () => {
+            await this._sequence.eraseContextTokenRanges([{
+                    start: 0,
+                    end: this._sequence.nextTokenIndex
+                }]);
+            const iterator = this._sequence.evaluate(resolvedInput, { _noSampling: true });
+            // eslint-disable-next-line @typescript-eslint/no-unused-vars
+            for await (const token of iterator) {
+                break; // only generate one token to get embeddings
+            }
+            // Get per-token embeddings. With pooling_type=none, getEmbedding(i)
+            // returns the (i-1)-th token's embedding via llama_get_embeddings_ith.
+            // Some tokens may fail (e.g., beyond model's position limit);
+            // fall back to zero vectors for those.
+            const allEmbeddings = [];
+            let zeroVector = null;
+            for (let i = 1; i <= resolvedInput.length; i++) {
+                try {
+                    const emb = this._llamaContext._ctx.getEmbedding(i);
+                    if (emb && emb.length > 0) {
+                        allEmbeddings.push(Array.from(emb));
+                        if (zeroVector === null) zeroVector = new Array(emb.length).fill(0);
+                        continue;
+                    }
+                } catch (_e) {
+                    // Fallback to zero vector
+                }
+                if (zeroVector !== null) {
+                    allEmbeddings.push([...zeroVector]);
+                }
+            }
+            return allEmbeddings;
+        });
+    }
+
     async dispose() {
         await this._disposeAggregator.dispose();
     }
diff --git a/node_modules/node-llama-cpp/dist/evaluator/LlamaRankingContext.js b/node_modules/node-llama-cpp/dist/evaluator/LlamaRankingContext.js
index cc77cce..ad794f0 100644
--- a/node_modules/node-llama-cpp/dist/evaluator/LlamaRankingContext.js
+++ b/node_modules/node-llama-cpp/dist/evaluator/LlamaRankingContext.js
@@ -9,11 +9,15 @@ export class LlamaRankingContext {
     /** @internal */ _llamaContext;
     /** @internal */ _template;
     /** @internal */ _sequence;
+    /** @internal */ _relevanceIndex;
+    /** @internal */ _isTwoClassClassifier;
     /** @internal */ _disposeAggregator = new AsyncDisposeAggregator();
     onDispose = new EventRelay();
-    constructor({ _llamaContext, _template }) {
+    constructor({ _llamaContext, _template, _relevanceIndex, _isTwoClassClassifier }) {
         this._llamaContext = _llamaContext;
         this._template = _template;
+        this._relevanceIndex = _relevanceIndex ?? 0;
+        this._isTwoClassClassifier = _isTwoClassClassifier ?? false;
         this._sequence = this._llamaContext.getSequence();
         this._disposeAggregator.add(this._llamaContext.onDispose.createListener(() => {
             void this._disposeAggregator.dispose();
@@ -132,12 +136,29 @@ export class LlamaRankingContext {
             for await (const token of iterator) {
                 break; // only generate one token to get embeddings
             }
-            const embedding = this._llamaContext._ctx.getEmbedding(input.length, 1);
-            if (embedding.length === 0)
-                return 0;
-            const logit = embedding[0];
-            const probability = logitToSigmoid(logit);
-            return probability;
+            // Reranker models come in two architectures:
+            // 1. Two-class classifier (e.g., Qwen3-Reranker): C++ does cls_out projection + softmax,
+            //    producing [P(label_0), P(label_1)] in [0,1] range summing to 1.
+            //    We detect this by the presence of "classifier.output_labels" in GGUF metadata.
+            // 2. Single regression output (e.g., bge-reranker-v2-m3): C++ does cls_out projection
+            //    only, outputting a raw logit (can be negative). We apply sigmoid in JS.
+            if (this._isTwoClassClassifier) {
+                // Two-class classifier path (Qwen3-Reranker style)
+                const nClsOut = 2;
+                const embedding = this._llamaContext._ctx.getEmbedding(input.length, nClsOut);
+                if (embedding.length < 2)
+                    return 0;
+                const probability = embedding[this._relevanceIndex]; // P(yes) = relevance score
+                return Math.max(0, Math.min(1, probability));
+            } else {
+                // Single regression path (bge-reranker style)
+                const embedding = this._llamaContext._ctx.getEmbedding(input.length, 1);
+                if (embedding.length === 0)
+                    return 0;
+                const logit = embedding[0];
+                const probability = logitToSigmoid(logit);
+                return probability;
+            }
         });
     }
     /** @internal */
@@ -166,9 +187,33 @@ export class LlamaRankingContext {
             _embeddings: true,
             _ranking: true
         });
+
+        // Determine model architecture:
+        // - Two-class classifier (Qwen3-Reranker): has "classifier.output_labels" in GGUF metadata.
+        //   C++ does softmax, output is [P(no), P(yes)]. We find the "yes" index.
+        // - Single regression (bge-reranker): no output_labels, C++ outputs raw logit.
+        //   We apply sigmoid in JS (original behavior).
+        let isTwoClassClassifier = false;
+        let relevanceIndex = 0;
+        try {
+            const metadata = _model.fileInfo.metadata;
+            const arch = metadata?.general?.architecture;
+            const labels = arch ? (metadata)[arch]?.classifier?.output_labels : undefined;
+            if (Array.isArray(labels) && labels.length > 0) {
+                isTwoClassClassifier = true;
+                const yesIdx = labels.indexOf('yes');
+                if (yesIdx !== -1)
+                    relevanceIndex = yesIdx;
+            }
+        } catch (_e) {
+            // Fall back to single-regression mode if metadata is not accessible
+        }
+
         return new LlamaRankingContext({
             _llamaContext: llamaContext,
-            _template: resolvedTemplate
+            _template: resolvedTemplate,
+            _relevanceIndex: relevanceIndex,
+            _isTwoClassClassifier: isTwoClassClassifier
         });
     }
 }
